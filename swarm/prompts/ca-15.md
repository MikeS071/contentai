# ca-15: Final Audit — E2E Tests + Docs

## Context
All packages built. This is the final polish + integration test ticket.

## What to Build

### E2E Integration Test
One test that exercises the full lifecycle:
1. `contentai init` (mock LLM for voice/blueprint)
2. `contentai kb add-feed` (mock RSS server)
3. `contentai kb sync`
4. `contentai ideas --count 1` (mock LLM)
5. `contentai new test-article --from-idea 1`
6. `contentai draft test-article` (mock LLM)
7. `contentai qa test-article --auto-fix --approve` (mock LLM)
8. `contentai hero test-article` (mock image API)
9. `contentai publish test-article --approve` (mock HTTP publisher)
10. `contentai social test-article` (mock LLM)
11. `contentai schedule test-article`

All in a temp directory, all with mocked external services.

### Documentation
- `README.md` — project overview, install, quickstart, full command reference
- `docs/architecture.md` — package structure, data flow, extension points
- Ensure all cmd/*.go have proper Use/Short/Long descriptions

### Final Checks
- `go build ./...` clean
- `go test ./... -count=1` all pass
- `go vet ./...` clean
- No TODO comments left unresolved
- All files ≤400 lines

### Files
- `e2e_test.go` — integration test
- `README.md`
- `docs/architecture.md`

### Tests
- `TestE2ELifecycle` — full pipeline test as described above

Target: overall project coverage ≥70%.

---
## MANDATORY: TDD — write E2E test FIRST (it will fail), then fix any integration issues, then docs.
Commit: `feat(ca-15): E2E test + documentation` with overall coverage.
