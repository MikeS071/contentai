# ca-07: `contentai ideas` — Idea Generation

## Context
Phase 1+2 code merged. You have: config, LLM client, templates, content store, init wizard (voice.md+blueprint.md), KB (feeds+search).

Read existing code in `internal/` before writing.

## What to Build

`contentai ideas [--from-kb] [--from-conversations] [--count 5]`

### Process
1. Gather sources: KB articles (`internal/kb`), user notes, blueprint core ideas
2. Load `deep-post-ideas` template
3. Assemble context: voice.md + blueprint.md + recent KB content + notes
4. Send to LLM → get N structured idea outlines
5. Each outline: title, core paradox, transformation arc, key examples, actionable steps
6. Save batch to `content/ideas/<date>-batch.md`
7. Interactive picker: present ideas, user selects which to develop → creates content item via `contentai new`

### Files
- `internal/ideas/generator.go` — idea generation logic
- `internal/ideas/generator_test.go` — tests FIRST
- `cmd/ideas.go` — cobra command

### Tests (write FIRST)
- `TestGenerateIdeasFromKB` — mock LLM+KB, structured outlines returned
- `TestGenerateIdeasWithBlueprint` — blueprint core ideas included in context
- `TestSaveIdeasBatch` — saved to ideas/ with correct format
- `TestPickerCreatesContentItem` — selected idea creates content/<slug>/
- `TestCountFlag` — respects --count
- `TestNoKBGraceful` — works even with empty KB

Target: 75% coverage on `internal/ideas/`.

## Constraints
- Use existing `internal/llm.Client`, `internal/kb.Store`, `internal/templates.Engine`
- Use existing `internal/content.Store` for creating new items from picks
- Files ≤400 lines, no new external deps

---
## MANDATORY: Test-Driven Development Process
1. Read SPEC.md before writing any code
2. Write FAILING tests FIRST
3. Implement minimum code to pass tests
4. Quality gates: `go build`, `go test`, `go vet`, coverage target
5. Commit: `feat(ca-07): description` with `Tests: X passing, Coverage: Y%`
6. Do NOT commit if any test is failing
