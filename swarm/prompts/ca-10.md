# ca-10: `contentai hero` — Hero Image Generation

## Context
Phase 1 code merged. You have: config, content store.

## What to Build

`contentai hero <slug> [--regenerate]`

### Image Generation
- DALL-E API client (OpenAI images endpoint)
- Prompt from `hero-prompt` template + article title + blueprint themes
- 8 color palettes rotated by slug hash:
  1. Deep ocean (navy/teal) 2. Forest (green/gold) 3. Sunset (orange/purple)
  4. Arctic (ice blue/white) 5. Volcanic (red/black) 6. Desert (sand/terracotta)
  7. Nebula (purple/pink) 8. Circuit (green/dark grey)
- Title text overlay using Go's `image/draw` + embedded font
- LinkedIn variant: 1200x627 resize from 1792x1024 base

### Output
```
content/<slug>/
├── hero.png           # 1792x1024 base
├── hero-linkedin.png  # 1200x627 variant
└── meta.json          # updated with image paths
```

### Files
- `internal/hero/generator.go` — image gen orchestration
- `internal/hero/palette.go` — 8 palettes + hash rotation
- `internal/hero/overlay.go` — title text overlay
- `internal/hero/hero_test.go` — tests FIRST
- `cmd/hero.go` — cobra command

### Tests (write FIRST)
- `TestPaletteRotation` — same slug always gets same palette
- `TestPaletteDistribution` — different slugs get different palettes
- `TestBuildPrompt` — template + title + palette description assembled
- `TestOverlayText` — text rendered on image (check dimensions)
- `TestLinkedInResize` — output is 1200x627
- `TestSavesImages` — hero.png + hero-linkedin.png created
- `TestMetaUpdated` — meta.json has image paths
- `TestRegenerate` — --regenerate overwrites existing

Target: 70% coverage on `internal/hero/`.

Note: For the DALL-E API call, create a mockable `ImageGenerator` interface. Tests use mock. Real impl calls OpenAI.

---
## MANDATORY: Test-Driven Development Process
1. Read SPEC.md before writing any code. 2. Write FAILING tests FIRST. 3. Implement.
4. Quality gates: `go build`, `go test`, `go vet`, coverage target
5. Commit: `feat(ca-10): description` with `Tests: X passing, Coverage: Y%`
