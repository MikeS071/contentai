# ca-05: `contentai init` Full Wizard

## Context
You are building the `contentai init` command — the interactive onboarding wizard. This is Phase 2.

**Phase 1 code is already merged.** You have access to:
- `internal/config/` — config loader with TOML parsing (ca-01)
- `internal/llm/` — pluggable LLM client with OpenAI+Anthropic, streaming, prompt assembly (ca-02)
- `internal/templates/` — embedded prompt templates with local override support (ca-04)
- `cmd/root.go`, `cmd/init.go` — cobra CLI scaffold (ca-01)

Read these files to understand existing interfaces before writing new code.

## What to Build

The `contentai init [name]` command — a multi-step interactive wizard:

### Step 1: Create workspace
- Create `contentai.toml` with sensible defaults (LLM provider, model, content dir)
- Create directory structure: `content/`, `content/examples/`, `content/kb/`, `content/kb/blogs/`, `content/kb/notes/`

### Step 2: Collect example articles
- Prompt: "Paste 2-3 of your best articles (or paths to markdown files):"
- Accept both inline paste (delimited by `---END---`) and file paths
- Save to `content/examples/<N>.md`
- Minimum 1 article required, max 5

### Step 3: Run Perspective Architect
- Load `perspective-architect` template from template engine
- Send to LLM with example articles as context
- 5-phase discovery: Excavation → Research → Synthesis → Crystallization → Blueprint
- Save result to `content/blueprint.md`

### Step 4: Generate voice profile
- Load `voice-extractor` template
- Send to LLM with: example articles + blueprint
- Extract: tone, vocabulary patterns, sentence structure, perspective style, recurring themes
- Save to `content/voice.md`

### Step 5: Configure integrations
- Prompt for LLM API key (validate with test call)
- Prompt for image API key (optional)
- Prompt for publish endpoint URL (optional)
- Store keys using `api_key_cmd` pattern — **never store raw API keys in config files**

### Step 6: Configure KB feeds
- Prompt: "Add RSS feeds of blogs you follow (one per line, blank to finish):"
- Save to `content/kb/feeds.toml`

## Files to Create/Modify

### 1. `internal/init/wizard.go`
Wizard struct with injectable I/O + LLM + templates for testability.

### 2. `internal/init/wizard_test.go`
Tests FIRST:
- `TestWizardCreatesDirs` — workspace dirs created
- `TestWizardSavesExampleArticles` — file paths and inline paste both work
- `TestWizardRunsPerspectiveArchitect` — mock LLM, blueprint.md written
- `TestWizardGeneratesVoiceProfile` — mock LLM, voice.md written
- `TestWizardConfiguresAPIKeys` — api_key_cmd written, raw keys never stored
- `TestWizardSavesFeedsToml` — feeds saved correctly
- `TestWizardMinimumOneArticle` — error if no articles
- `TestWizardIdempotent` — running twice doesn't destroy existing content

Target: 80% coverage on `internal/init/`.

### 3. `cmd/init.go` — update to call wizard
### 4. `internal/init/feeds.go` — feeds.toml parser

## Constraints
- Use existing `internal/llm.Client` interface for all LLM calls
- Use existing `internal/templates.Engine` for loading prompts
- Interactive I/O via `bufio.Scanner` on Wizard's Stdin/Stdout (testable)
- Files ≤400 lines, no new external deps

---
## MANDATORY: Test-Driven Development Process
1. Read SPEC.md before writing any code
2. Write FAILING tests FIRST
3. Implement minimum code to pass tests
4. Quality gates before commit: `go build`, `go test`, `go vet`, coverage target
5. Commit: `feat(ca-05): description` with `Tests: X passing, Coverage: Y%`
6. Do NOT commit if any test is failing
