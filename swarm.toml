[project]
name = "contentai"
repo = "."
base_branch = "main"
max_agents = 5
min_ram_mb = 1024
prompt_dir = "swarm/prompts"
tracker = "swarm/tracker.json"

[backend]
type = "codex-tmux"
model = "o4-mini"
bypass_sandbox = true

[watchdog]
interval = "5m"
max_runtime = "45m"
max_retries = 2
